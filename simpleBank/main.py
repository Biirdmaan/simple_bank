import bank
"""

class Main:
    abc = ""

    if account.balance > 0:
        cash_back = f"You will receive {account.balance} from account: {account.number, account.account_type}"
        return cash_back
    elif account.balance == 0:
        message = f"Account balance is: {account.balance} in account: {account.number, account.account_type}\n" \
                  f"you will not receive or payback any cash."
        return message
    else:
        payback_cash = f"Account {account.account_number, account.account_type} is negative.\n" \
                       f"You owe the bank: {account.balance}"
        return payback_cash

"""